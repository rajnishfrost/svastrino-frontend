|Number|Menu Options / Page Name|Various elements to be added downwards ↓|Section 1|Section 2|Section 3|Section 4|Section 5|Section 6|Section 7|Section 8|Section 9|-|-|-|-|
|-|-|-|-|-|-|-|-|-|-|-|-|-|-|-|-|
|7.3|Settings|Overall section contains and feel  Following right ways >|Page header — names the account area and says what can be managed here|Tab bar — lets the visitor switch between their account details and their orders. Always shown.|Account — the profile header showing the photo, name and email, with the photo the visitor can change|Photo-trimming window — opens once a photo is picked, so it can be squared off before it is saved|Account messages — the red warning or green confirmation shown after any account change|Account details — the list of Name, Email, Account type, Phone and Password, each with its own action|Change your name — the small form that takes the place of the Name row while it is being edited|Change your phone number — the small form that takes the place of the Phone row while it is being edited|Set or change your password — the small form that takes the place of the Password row, with a live strength check|Orders — the please-wait message while the order list is being fetched, and the red message if it cannot be fetched|Orders — the list of everything the visitor has bought, or a message when there is nothing yet|Order details — the full breakdown of one purchase, with the invoice download|Order details — the message shown when the order asked for cannot be found|
|-|-|Section background|a plain narrow band at the top of the page|a row of tab labels with a thin line underneath; on a narrow screen it slides sideways|a white card with a line under its top row|a small white box in the middle of the screen over a dark see-through cover|a thin rounded coloured band inside the account card|a list of rows separated by thin lines|a small form set into the list of rows, with a line under it|a small form set into the list of rows, with a line under it|a small form set into the list of rows, with a line under it|a white card|a white card holding a list of clickable rows separated by thin lines|a white card with a list of rows and, below it, a shaded box holding the price breakdown|a white card|
|-|-|Type of animation to drive this section|-|the tab you click turns a highlight colour with a line under it, and the panel below changes|the photo dims a little when you point at it|the photo can be dragged around to reposition it and zoomed in or out with the scroll wheel, the slider or the plus and minus buttons|-|clicking a row's action swaps that one row for a small edit form|-|clicking the country flag opens a list of countries to pick from|the strength bar under the new password grows and changes colour as you type|-|each row lightly shades when you point at it|-|-|
|-|-|Text before content|-|-|-|-|-|-|-|-|-|-|-|‹ Back to orders|‹ Back to orders|
|-|-|Content Type|Text|Text|Text + Image|Form|Text|Text|Form|Form|Form|Text|Cards|Text|Text|
|-|-|Images / Video (In drive)|-|-|The visitor's own profile photo as a small square. When there is no photo, or it cannot be loaded, a dark blue square with the first letter of their name (or email) is shown instead. A small camera badge sits in the bottom-right corner of the photo. Photos can be PNG, JPG or WEBP.|The photo the visitor has just picked, shown in a square preview|-|-|-|Small country flags beside the phone number box|-|-|-|-|-|
|-|-|Text content|Heading: “Settings”. Below it: “Manage your account details, security and orders.”|Tab labels: “Account”, “Orders”. The Account tab is the one open when the page first loads.|Comes from the visitor's own account: their name in large text, or the words “Your account” when no name is saved. Under it, their email address in grey.|Title: “Adjust your photo”. Under the zoom control: “Drag to reposition · scroll or use the slider to zoom”. The zoom control is a slider labelled “Zoom”.|Red warning messages: “Name is required”, “New passwords do not match”, “Password required”, “Minimum 8 characters”, “Don’t use your name in the password”, “Use letters, numbers & a symbol”. Anything else is the message sent back by the system, word for word. · Green confirmation messages: “Profile photo updated.”, “Name updated.”, “Phone updated — verify it when you get the option.”, “Password changed.” when the visitor already had a password, or “Password set.” when they did not.|Comes from the visitor's own account. Row labels: “Name”, “Email”, “Account type”, “Phone”, “Password”. · Name: the saved name, or a dash when nothing is saved. · Email: the address used to sign in — it cannot be changed here; a tag beside it reads “✓ Verified” or “Unverified”. · Account type: the kind of account with a capital first letter, and “Student” when nothing is set — only an admin can change this. · Phone: the saved number, or the words “Not added”; when a number exists a tag reads “✓ Verified” or “Unverified”. · Password: eight dots when a password is set, otherwise the words “Not set”.|Field label: “Name”. One typing box, already filled with the current name, showing the grey hint “Your full name”, with a limit of 60 letters. The cursor is placed in it automatically.|Field label: “Phone”. One phone number box that starts on India, already filled with the saved number, or showing “+91” when no number is saved.|Field label: “Change password” when the visitor already has one, otherwise “Set password”. · Typing boxes, each with its wording shown as grey hint text inside: “Current password” — only shown when the visitor already has a password; “New password”; “Confirm new password”. · Under the new password box a strength note reads one of: “Too short”, “Weak”, “Fair”, “Good”, “Strong”. It stays blank while the box is empty. A password that contains part of the visitor's own name can never rise above “Weak”.|While waiting: “Loading…” in grey. If something goes wrong: the message sent back by the system, word for word, in a red band above the rest of the panel.|Heading in both cases: “Orders”. · When there are no orders: “You haven't placed any orders yet.” followed by the link “Explore Nirmaan →”. · When there are orders, the list comes from the database — every purchase this visitor has made. Each row shows what was bought, for example “Nirmaan — Launch”; under it the receipt number, or the word “Pending” when no receipt has been raised yet, then the date it was placed, for example “12 Aug 2026”; the amount in rupees; and a coloured tag reading “Paid” in green, “Refunded” in grey, “Failed” in amber or “Pending” in amber.|Heading: what was bought, for example “Nirmaan — Launch”, with the status tag beside it reading “Paid”, “Refunded”, “Failed” or “Pending”. · Detail rows: “Receipt No” — the receipt number, or a dash; “Date” — the date it was paid, or the date it was placed, for example “12 Aug 2026”; “Product” — the course name; “Package” — the plan name, shown only when there is one, with “ (upgrade)” added when this purchase was an upgrade; “Payment” — the currency and the status, for example “INR · PAID”. · Price breakdown, all amounts in rupees: “Package price” — the full price; “Early-bird discount” — the amount taken off, in green with a minus in front, only when an early-bird price applied; “Coupon {code}” — the coupon amount taken off, in green, only when a coupon was used (just “Coupon” when no code was stored); “Upgrade credit (already paid)” — the money already paid on the earlier plan, in green, only on an upgrade; “Total paid” — always shown, in bold on the last row.|“That order could not be found.”|
|-|-|Button name|-|Account > shows the account details panel at /settings ; Orders > shows the list of orders at /settings?tab=orders|Profile photo > opens the file picker on the visitor's device, then opens the photo-trimming window — switched off while a photo is already being uploaded|− > zooms the photo out ; + > zooms the photo in ; Zoom slider > makes the photo bigger or smaller ; Save photo > saves it as the new profile photo, closes the window and shows the message “Profile photo updated.” — the label changes to “Saving…” while it saves ; Cancel > closes the window without saving|-|Edit (Name row) > opens the small form to change the name ; Edit (Phone row) > opens the form to change the number — only when a number is already saved ; Add (Phone row) > opens the form to add a number — only when no number is saved ; Change (Password row) > opens the password form — only when a password is already set ; Set (Password row) > opens the password form — only when no password is set. The Email and Account type rows have no button.|Save > saves the new name, closes the form and shows “Name updated.” — the label changes to “Saving…” while it saves; if the box is left empty it shows the warning “Name is required” ; Cancel > closes the form without saving|Save > saves the number, closes the form and shows “Phone updated — verify it when you get the option.” — the label changes to “Saving…” while it saves ; Cancel > closes the form without saving|Save > checks the password, saves it, closes the form and shows “Password changed.” or “Password set.” — the label changes to “Saving…” while it saves; problems appear in the red band as “Minimum 8 characters”, “Don’t use your name in the password”, “Use letters, numbers & a symbol” or “New passwords do not match” ; Cancel > closes the form without saving|-|Explore Nirmaan → > /skill-build/nirmaan — only when the visitor has no orders ; Any order row (the whole row can be clicked) > opens the full details of that order in place of the list|‹ Back to orders > returns to the list of orders ; Download invoice > opens a printable invoice in a new tab and starts the print box; if the browser blocks the new tab the invoice downloads as a file instead. The invoice carries “Svastrino Consultancy Services”, “Soch Se Vikas”, “Thane · Mumbai · Dharamshala, India”, “support@svastrino.com · svastrino.com”, the heading “INVOICE”, the “Billed to” and “Invoice details” blocks, a table with “Description” and “Amount”, the same price breakdown ending in “Total paid”, and its own “Download / Print PDF” button|‹ Back to orders > returns to the list of orders|
|-|-|Text after content|-|-|-|-|-|-|-|-|-|-|-|-|-|
|-|-|-|-|-|-|-|-|-|-|-|-|-|-|-|-|